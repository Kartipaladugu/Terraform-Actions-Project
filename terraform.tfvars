# Terraform variable values
# Note: credentials_json is set via environment variable TF_VAR_credentials_json in CI
# For local testing, uncomment and set the path to your credentials JSON file
# credentials_json = file("path/to/credentials.json")